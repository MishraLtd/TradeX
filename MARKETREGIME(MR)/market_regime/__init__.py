"""
TradeX Market Regime Model
===========================
Pure-stdlib, dependency-free package (no numpy/pandas/pydantic required) so it
stays lightweight and trivially deployable inside the low-capital TradeX stack.

Entry point:
    from market_regime.inference import RegimeEngine
"""

__version__ = "0.1.0"  # model_version root; see config.MODEL_VERSION
